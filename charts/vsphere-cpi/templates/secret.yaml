apiVersion: v1
kind: Secret
metadata:
  name: {{ template "common.names.fullname" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: cloud-controller-manager
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: Opaque
stringData:
{{- if not .Values.config.secretsInline }}
  {{- range $k, $v := .Values.config.vcenter }}
    {{- if not (or $v.secretName $v.secretNamespace)}}
      {{- if and (and $v.server (or $v.user $v.username)) $v.password}}
      {{ $v.server}}.username: {{$v.password | quote}}
      {{- end }}
    {{- end }}
  {{- end }}
{{- else }}
  vsphere.conf: |
    {{- if .Values.config.global }}
    global:
    {{- include "common.tplvalues.render" ( dict "value" ( omit .Values.config.global "secretName" "secretNamespace" ) "context" $ ) | nindent 6 }}
    {{- end }}
    {{- if .Values.config.vcenter }}
    vcenter:
      {{- range $k, $v := .Values.config.vcenter }}
      {{ $k }}:
        {{- $v | toYaml | nindent 8}}
      {{- end }}
    {{- end }}
    {{- if .Values.config.labels }}
    labels:
    {{- include "common.tplvalues.render" ( dict "value" .Values.config.labels "context" $ ) | nindent 6 }}
    {{- end }}
{{- end }}
